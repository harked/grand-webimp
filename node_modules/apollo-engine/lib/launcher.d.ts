/// <reference types="@types/node" />
import { EventEmitter } from 'events';
import { EngineConfig, LauncherOptions, ListeningAddress } from './types';
export declare class ApolloEngineLauncher extends EventEmitter {
    private config;
    private binary;
    private child;
    private eventStopper?;
    constructor(config: EngineConfig);
    start(options?: LauncherOptions): Promise<ListeningAddress>;
    stop(): Promise<void>;
    private emitRestarting(error);
    private setUpStopEvent(event);
    private setUpStopEvents(events);
}
export declare function joinHostPort(host: string, port: number): string;
